.page { background: transparent; min-height: 100vh; }
.loading { text-align: center; padding: 60px 0; color: #999; }

/* 顶部搜索框（与前端文案一致） */
.products-search-wrap { padding: 20rpx 24rpx 18rpx; }
.products-search-inner { display: flex; align-items: center; gap: 16rpx; background: #2d2d33; border-radius: 20rpx; padding: 18rpx 22rpx; }
.products-search-icon { color: #9ca3af; font-size: 28rpx; }
.products-search-input { flex: 1; min-width: 0; color: #f3f4f6; font-size: 30rpx; }

/* 第二行：选择商品（与网页一致） */
.product-select-row {
  background: rgba(255,255,255,0.95);
  padding: 24rpx 32rpx;
  border-bottom: 1rpx solid rgba(0,0,0,0.06);
}
.select-product-field {
  display: flex;
  align-items: center;
}
.select-label { font-size: 28rpx; color: #333; margin-right: 16rpx; flex-shrink: 0; }
.select-value { flex: 1; font-size: 28rpx; color: #666; }
.select-arrow { font-size: 32rpx; color: #999; }

/* 与 H5 一致：左侧二级列表 + 右侧内容 */
.products-layout-mp {
  display: flex;
  flex-direction: row;
  align-items: stretch;
  min-height: 400rpx;
  background: #ffffff;
  border-bottom: 1rpx solid rgba(0, 0, 0, 0.08);
}

.product-sidebar-mp {
  width: 200rpx;
  flex-shrink: 0;
  max-height: 70vh;
  background: #eef0f3;
  border-right: 1rpx solid rgba(0, 0, 0, 0.08);
  padding: 16rpx 0;
  box-sizing: border-box;
}

/* 一级种类：较大字；二级种类、商品行见 .sub / .product-sub（小约 4rpx≈2 号） */
.sidebar-item-mp {
  margin: 0 12rpx 12rpx;
  padding: 16rpx 12rpx;
  border-radius: 999rpx;
  font-size: 28rpx;
  font-weight: 500;
  color: #4b5563;
  line-height: 1.35;
}

.sidebar-item-mp.header {
  font-size: 28rpx;
  font-weight: 700;
  color: #6b7280;
  border-radius: 16rpx;
}

.sidebar-item-mp.sub {
  padding: 14rpx 12rpx;
  font-size: 24rpx;
}

.sidebar-item-mp.product-sub {
  padding: 12rpx 12rpx 12rpx 20rpx;
  margin-left: 6rpx;
  border-radius: 16rpx;
  border-left: 4rpx solid rgba(185, 28, 28, 0.3);
  font-size: 24rpx;
}

.sidebar-item-mp.active {
  background: rgba(255, 183, 77, 0.45);
  color: #111827;
  font-weight: 600;
}

.sidebar-name-mp {
  display: block;
  width: 100%;
  word-break: break-all;
}

.product-main-mp {
  flex: 1;
  min-width: 0;
  background: #fafafa;
}
.product-main-scroll {
  max-height: 70vh;
  box-sizing: border-box;
  padding: 10px;
}

/* Hero */
.hero-block { background: #fff; margin-bottom: 10px; }
.hero { position: relative; height: 220px; overflow: hidden; }
.hero-img { width: 100%; height: 100%; }
.hero-fallback { height: 100%; display: flex; flex-direction: column; align-items: center; justify-content: center; }
.hero-icon-img { width: 64px; height: 64px; }
.hero-emoji { font-size: 56px; }
.hero-name { font-size: 22px; color: #fff; font-weight: 600; margin-top: 8px; }
.play-btn { position: absolute; top: 50%; left: 50%; transform: translate(-50%,-50%); width: 50px; height: 50px; background: rgba(0,0,0,0.5); border-radius: 50%; color: #fff; font-size: 22px; display: flex; align-items: center; justify-content: center; line-height: 50px; text-align: center; }

/* Media（红框区域居中，与前台一致） */
.hero-media { padding: 16px; text-align: center; }
.hero-media-title { display: block; font-size: 16px; font-weight: 600; color: #1a1a1a; margin-bottom: 12px; }
.media-scroll { white-space: nowrap; display: flex; flex-direction: row; justify-content: center; }
.media-card { display: inline-block; width: 130px; margin-right: 10px; vertical-align: top; flex-shrink: 0; }
.media-thumb-wrap { width: 130px; height: 160px; border-radius: 10px; overflow: hidden; position: relative; background: #1a1a1a; }
.media-thumb { width: 100%; height: 100%; }
.media-placeholder { width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; font-size: 28px; background: #2a2a2a; }
.media-play { position: absolute; top: 40%; left: 50%; transform: translate(-50%,-50%); width: 36px; height: 36px; background: rgba(0,0,0,0.5); border-radius: 50%; color: #fff; font-size: 14px; text-align: center; line-height: 36px; }
.media-label-overlay { position: absolute; bottom: 0; left: 0; right: 0; padding: 20px 8px 8px; background: linear-gradient(transparent, rgba(0,0,0,0.7)); }
.media-label-overlay text { font-size: 13px; font-weight: 600; color: #fff; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; display: block; }

/* Cards */
.card { background: #fff; margin: 10px 12px; border-radius: 12px; padding: 16px; }
.title { display: block; font-size: 16px; font-weight: 600; color: #1a1a1a; margin-bottom: 12px; }

.help-row { display: flex; justify-content: space-between; align-items: center; padding: 14px 0; border-bottom: 1px solid #f0f0f0; }
.help-row:last-child { border-bottom: none; }
.help-title { font-size: 14px; color: #333; }
.help-arrow { font-size: 18px; color: #ccc; }

.price-row-mp {
  margin: 0 12px 10px;
  background: #fff;
  border-radius: 12px;
  padding: 14px 16px;
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 8px 14px;
  font-size: 15px;
}
.price-row-mp .pr-label { font-size: 13px; color: #6b7280; }
.price-row-mp .pr-val { font-weight: 700; color: #b91c1c; font-size: 18px; }
.price-row-mp .pr-pts { font-weight: 600; color: #111; }

.footer-shell {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 99;
  width: 100%;
  max-width: 750rpx;
  margin: 0 auto;
  padding: 0 24rpx;
  box-sizing: border-box;
}
.footer-inner {
  display: flex;
  gap: 16rpx;
  align-items: stretch;
  padding: 20rpx 0;
  padding-bottom: calc(20rpx + env(safe-area-inset-bottom));
  background: rgba(255, 255, 255, 0.96);
  border-radius: 24rpx 24rpx 0 0;
  box-shadow: 0 -1rpx 8rpx rgba(0, 0, 0, 0.06);
}
.fb-btn {
  flex: 1;
  min-width: 0;
  font-size: 28rpx;
  padding: 20rpx 12rpx;
  border-radius: 999rpx;
  border: none;
  margin: 0;
  line-height: 1.3;
}
.fb-cart {
  background: #f3f4f6;
  color: #374151;
}
.fb-buy {
  background: #fee2e2;
  color: #b91c1c;
  font-weight: 600;
}
.fb-add {
  background: #b91c1c;
  color: #fff;
  font-weight: 600;
}

.empty-hint { text-align: center; padding: 80px 0; color: #999; font-size: 14px; }
.tab-bar-placeholder { height: 120rpx; }

.viewer-3d-wrap {
  width: 100%;
  height: 70vh;
  background: #1a1a2e;
}
